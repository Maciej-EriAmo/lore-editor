# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# Build / packaging
dist/
build/
*.build/

# Env / secrets
.env

# Project runtime data (created when editor is run from repo)
*.kafd
*.meta.json
.lore-project
.lore-history/
shards/
peers.json

# Local tooling dumps (not part of the app)
mcps/
proca/

# Vendored dependency (tracked separately / optional)
vendor/Astraedit/
